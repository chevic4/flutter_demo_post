import 'package:flutter/material.dart';

class PostInfoScreen extends StatelessWidget {
  const PostInfoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('post info'));
  }
}
